(function(){
  const isRider=location.pathname.includes('rider'),kind=isRider?'rider':'merchant',tokenKey=isRider?'khalasaRiderToken':'khalasaMerchantToken',API='https://khalasa-api.onrender.com/v1';
  const types=isRider?{national_id:'بطاقة الرقم القومي',driving_license:'رخصة القيادة',vehicle_license:'رخصة المركبة',criminal_record:'صحيفة الحالة الجنائية'}:{commercial_register:'السجل التجاري',tax_card:'البطاقة الضريبية',owner_id:'بطاقة هوية المالك',activity_license:'ترخيص النشاط'};
  const style=document.createElement('style');style.textContent='.pd-open{position:fixed;left:14px;bottom:132px;z-index:9000;border:0;border-radius:13px;padding:12px 16px;background:#123f31;color:#fff;font-weight:800}.pd-modal{position:fixed;inset:0;z-index:15000;background:#10251b99;display:none;overflow:auto;padding:18px;direction:rtl;font-family:Cairo,Arial}.pd-modal.show{display:block}.pd-card{max-width:680px;margin:auto;background:#fff;border-radius:18px;padding:20px}.pd-row{display:flex;gap:9px;align-items:center;justify-content:space-between;flex-wrap:wrap}.pd-form{display:grid;gap:10px;margin:14px 0}.pd-form select,.pd-form input{padding:12px;border:1px solid #cbdad3;border-radius:9px;background:#fff}.pd-btn{border:0;border-radius:9px;padding:10px 14px;background:#12805a;color:#fff;font-weight:800}.pd-btn.alt{background:#edf4f0;color:#164b39}.pd-doc{border:1px solid #dae6e0;border-radius:12px;padding:12px;margin:8px 0}.pd-status{display:inline-block;border-radius:20px;padding:4px 9px;background:#fff2d5;color:#8b6200;font-size:11px}.pd-status.approved{background:#e5f8ef;color:#08734e}.pd-status.rejected{background:#ffebe7;color:#b42318}.pd-note{font-size:12px;color:#6e7c76}.pd-error{color:#b42318}';document.head.appendChild(style);
  document.body.insertAdjacentHTML('beforeend','<button id="pdOpen" class="pd-open">🛡️ مركز التوثيق</button><section id="pdModal" class="pd-modal"><div class="pd-card"><div class="pd-row"><h2>المستندات والتوثيق</h2><button id="pdClose" class="pd-btn alt">إغلاق</button></div><p class="pd-note">الملفات خاصة، ولا يمكن عرضها إلا للإدارة عبر رابط مؤقت.</p><form id="pdForm" class="pd-form"><select id="pdType"></select><input id="pdFile" type="file" accept="image/jpeg,image/png,application/pdf" required><button class="pd-btn">رفع المستند للمراجعة</button></form><p id="pdMessage" class="pd-note"></p><div id="pdList"></div></div></section>');
  const $=id=>document.getElementById(id),token=()=>sessionStorage.getItem(tokenKey)||'';Object.entries(types).forEach(([value,label])=>{const option=document.createElement('option');option.value=value;option.textContent=label;$('pdType').append(option)});
  async function request(path,options={}){const headers=new Headers(options.headers||{});headers.set('Content-Type','application/json');headers.set('Authorization','Bearer '+token());const response=await fetch(API+path,{...options,headers}),body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||'FAILED');return body;}
  function toBase64(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(file)})}
  async function load(){const box=$('pdList');if(!token()){box.textContent='سجّل الدخول أولًا لرفع المستندات.';return}try{const data=await request('/'+kind+'/documents');box.replaceChildren();for(const item of data.documents||[]){const card=document.createElement('article');card.className='pd-doc';const row=document.createElement('div');row.className='pd-row';const title=document.createElement('b');title.textContent=types[item.document_type]||item.document_type;const status=document.createElement('span');status.className='pd-status '+item.verification;status.textContent={pending:'قيد المراجعة',approved:'معتمد ✓',rejected:'مرفوض'}[item.verification]||item.verification;row.append(title,status);card.append(row);if(item.review_note){const note=document.createElement('p');note.className='pd-note '+(item.verification==='rejected'?'pd-error':'');note.textContent='ملاحظة الإدارة: '+item.review_note;card.append(note)}box.append(card)}if(!box.children.length)box.textContent='لم يتم رفع مستندات بعد.'}catch(_){box.textContent='تعذر تحميل المستندات.'}}
  $('pdOpen').onclick=()=>{$('pdModal').classList.add('show');load()};$('pdClose').onclick=()=>$('pdModal').classList.remove('show');
  $('pdForm').onsubmit=async event=>{event.preventDefault();const file=$('pdFile').files[0];if(!file)return;if(file.size>4194304){$('pdMessage').textContent='الحد الأقصى للملف 4 ميجابايت.';return}const button=event.target.querySelector('button');button.disabled=true;$('pdMessage').textContent='جارٍ رفع المستند بشكل آمن…';try{await request('/'+kind+'/documents',{method:'POST',body:JSON.stringify({documentType:$('pdType').value,fileName:file.name,mimeType:file.type,dataBase64:await toBase64(file)})});event.target.reset();$('pdMessage').textContent='تم الرفع وأصبح المستند قيد المراجعة.';await load()}catch(_){$('pdMessage').textContent='تعذر رفع المستند. تحقق من النوع والحجم وإعدادات التخزين.'}finally{button.disabled=false}};
})();
