(function(){
  const style=document.createElement('style');style.textContent='.kco-open{position:fixed;right:14px;bottom:18px;z-index:90;border:0;border-radius:14px;padding:13px 17px;background:#147e59;color:#fff;font:800 14px Cairo;box-shadow:0 9px 24px #1234}.kco-modal{position:fixed;inset:0;z-index:10000;background:#10251b88;display:none;align-items:center;justify-content:center;padding:18px;direction:rtl;font-family:Cairo,Arial}.kco-modal.show{display:flex}.kco-card{width:min(620px,100%);max-height:85vh;overflow:auto;background:#fff;border-radius:18px;padding:20px}.kco-row{display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap}.kco-order{border:1px solid #d8e6df;border-radius:12px;padding:13px;margin:10px 0}.kco-btn{border:0;border-radius:9px;padding:10px 13px;background:#147e59;color:#fff;font-weight:800}.kco-btn.alt{background:#eef4f1;color:#164b39}.kco-code{font-size:25px;font-weight:900;letter-spacing:5px;color:#164b39;margin:10px 0}.kco-note{font-size:13px;color:#6c7b74}';document.head.appendChild(style);
  document.body.insertAdjacentHTML('beforeend','<button id="kcoOpen" class="kco-open">طلباتي ورمز التسليم</button><section id="kcoModal" class="kco-modal"><div class="kco-card"><div class="kco-row"><h2>طلباتي</h2><button id="kcoClose" class="kco-btn alt">إغلاق</button></div><p class="kco-note">لا تعطِ رمز التسليم إلا للمندوب بعد استلام الطلب.</p><div id="kcoOrders"></div></div></section>');
  const $=id=>document.getElementById(id),labels={awaiting_merchant:'بانتظار المحل',preparing:'قيد التجهيز',awaiting_rider:'بانتظار المندوب',assigned:'تم إسناده للمندوب',picked_up:'قيد التوصيل',delivered:'تم التسليم'};
  function el(tag,text){const x=document.createElement(tag);x.textContent=String(text??'');return x}
  async function load(){const box=$('kcoOrders');box.textContent='جارٍ التحميل…';try{const data=await window.KhalasaApi.request('/customer/orders');box.replaceChildren();for(const order of data.orders||[]){const card=document.createElement('article');card.className='kco-order';card.append(el('b',order.public_code),el('p',labels[order.status]||order.status));if(['assigned','picked_up'].includes(order.status)){const button=el('button','إصدار رمز تسليم جديد');button.className='kco-btn';button.onclick=()=>reset(order,card,button);card.append(button)}box.append(card)}if(!box.children.length)box.append(el('p','لا توجد طلبات.'))}catch(error){box.textContent=error.message==='AUTH_REQUIRED'?'سجّل دخول العميل أولاً.':'تعذر تحميل الطلبات.'}}
  async function reset(order,card,button){button.disabled=true;try{const data=await window.KhalasaApi.request(`/customer/orders/${encodeURIComponent(order.id)}/reset-delivery-code`,{method:'POST',body:'{}'});const code=el('div',data.order.deliveryOtp);code.className='kco-code';card.append(el('p','رمز التسليم لهذا الطلب:'),code);button.remove()}catch(_){button.disabled=false;card.append(el('p','تعذر إصدار الرمز. حدّث الصفحة وحاول مرة أخرى.'))}}
  $('kcoOpen').onclick=()=>{$('kcoModal').classList.add('show');load()};$('kcoClose').onclick=()=>$('kcoModal').classList.remove('show');
})();
